It is a project that imitates an application that allows people to rent cars from certain companies.

The interface is fully console-based.

The application is based on connection with H2 DB

Allows you to:
* Company addition / removal
* Adding / removing cars belonging to a specific company
* Add / remove users
* Renting / returning a specific car of a given company by the user 
